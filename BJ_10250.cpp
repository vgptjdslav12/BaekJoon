/*#include <iostream>

using namespace std;

int main()
{
	int t, h, w, n, temp, div, rem;
	cin >> t;
	for (int i = 0; i < t; i++)
	{
		cin >> h >> w >> n;
		div = n / h;
		rem = n % h;
		if (rem == 0)
		{
			temp = h * 100 + div;
		}
		else temp = (rem * 100) + (div + 1);

		cout << temp << endl;
	}
	
	return 0;
}*/