/*#include <iostream>
using namespace std;

int main()
{
	int x, y, w, h, temp, ax , ay;
	cin >> x >> y >> w >> h;
	ax = (abs(w - x) > x) ? x : abs(w - x);
	ay = (abs(h - y) > y) ? y : abs(h - y);
	temp = ax > ay ? ay : ax;
	cout << temp;
}*/